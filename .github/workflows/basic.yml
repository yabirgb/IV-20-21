name: Exploraciones varias
on: [push, pull_request]

jobs:
  list-perl-modules:
    runs-on: ubuntu-latest
    steps:
      - name: lista módulos de Perl
        run: grep -RhIP '^package [A-Z][\w:]+;' `perl -e 'print join " ",@INC'` | sed 's/package //' | sort | uniq

